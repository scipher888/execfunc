# ExecFunc Consistency Review

- No remaining `family of 7` references found in the five reviewed files. Explicit family-size references already say `family of six` in `README.md:65` and `OPEN-SOURCE-PATH.md:14`. No fix needed.
- `OPEN-SOURCE-PATH.md:111` says `including a minor` (singular), but the Users section now lists two minors: `E (17)` and `T (13)` at `OPEN-SOURCE-PATH.md:61-62`. Fix: change this to `including minors` or `including two minors`.
- `OPEN-SOURCE-PATH.md:115`, `OPEN-SOURCE-PATH.md:120-122`, and `OPEN-SOURCE-PATH.md:132-134` give assent, visibility, and escalation protections only for `E`. `T (13)` is now a named user at `OPEN-SOURCE-PATH.md:62` and needs the same protections. Fix: rewrite these bullets to apply to both minors generically, or add parallel `T`-specific bullets.
- `OPEN-SOURCE-PATH.md:297` and `OPEN-SOURCE-PATH.md:310` still operationalize `E` as the only child/minor rollout target, while the Users section now scopes the family build to all six family members at `OPEN-SOURCE-PATH.md:56-64`. Fix: either add `T` where intended in success criteria/timeline, or clarify that the Users section lists eligible participants while the initial rollout remains a smaller subset.
- `REVIEW-Codex.md:22` still says `a minor`, and `REVIEW-Codex.md:25-28` contains stale `OPEN-SOURCE-PATH.md` line refs after the Users-section expansion. Current targets are `OPEN-SOURCE-PATH.md:61-62` for the minor users, `OPEN-SOURCE-PATH.md:79-82` for family escalation, `OPEN-SOURCE-PATH.md:189-193` for per-family-member results, and `OPEN-SOURCE-PATH.md:209` for `Results and evidence (anonymized)`. Fix: update both the singular wording and the citations.
- `REVIEW-FINAL-Codex.md:24` is now stale: it says `OPEN-SOURCE-PATH.md:116-117` allows parents to see minors' aggregate trend data by default, but the current rule at `OPEN-SOURCE-PATH.md:120-122` says nothing is shared by default and the minor chooses what to share. Fix: rewrite or remove this contradiction note so it matches the current minors policy.
